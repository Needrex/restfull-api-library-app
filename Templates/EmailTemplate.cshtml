@model RestApiApp.Dtos.EmailContent

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Verifikasi Email</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f6f6f6;
            padding: 20px;
        }
        .container {
            background-color: #ffffff;
            max-width: 600px;
            margin: auto;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0px 0px 8px rgba(0,0,0,0.1);
        }
        .button {
            background-color: #007bff;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 5px;
            display: inline-block;
            margin-top: 20px;
        }
        .footer {
            margin-top: 40px;
            font-size: 12px;
            color: #999999;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Halo, @Model.EmailRecipient!</h2>
        <p>Terima kasih telah mendaftar di layanan kami pada <strong>@Model.JoinDate.ToString("dd MMMM yyyy")</strong>.</p>
        <p>Silakan klik tombol di bawah ini untuk memverifikasi akun Anda:</p>

        <a class="button" href="@Model.VerifyLink">Verifikasi Email</a>

        <p>Jika Anda tidak mendaftar layanan ini, abaikan saja email ini.</p>

        <div class="footer">
            &copy; @DateTime.Now.Year RestApiApp. Semua hak dilindungi.
        </div>
    </div>
</body>
</html>
